# Sprout Work — 사용성 개선 계획서

_작성: 2026-07-30 · 실사용 테스트 기반_

## 목표
많은 사람들이 **불편함 없이 업무를 기록**하는 것. 그 관점에서 핵심 흐름
(작업 기록의 간편함, 시간 기록의 정확성, 시간 초과·부재중 관리, PC 복귀 후 시간 보정)과
모바일/데스크톱 UI를 실제로 사용하며 점검했다.

## 테스트한 흐름
- 신규 작업 시작(입력 → Start), 시간 카운트다운, 일시정지/재개
- 시간 초과(overtime) 알림·탭 제목 깜빡임
- 부재중(idle) 감지 → 자동 일시정지 + 전환 모달
- PC 복귀 후 시간 보정(자리비움 시간 포함/제외)
- 하위작업(트리) 생성(Switch) 및 중첩
- 내일 계획(미완료 가지) + Todo 연동
- 모바일/데스크톱 반응형 레이아웃

---

## 이번에 수정 완료 (배포됨)

| # | 문제 | 심각도 | 조치 |
|---|------|--------|------|
| 1 | **복귀-보정 기능이 실제 상황에서 작동 안 함.** 컴퓨터가 꺼진 뒤 세션은 여전히 `active`(인터벌 열림)인데, 코드는 `paused` 세션만 찾아 모달이 뜨지 않았음. 절전→깨어남(리로드 없음)도 미감지. | 중대 | active·열린 인터벌 케이스 처리로 재설계. 리로드·visibilitychange·tick의 벽시계 점프까지 3중 트리거. 모달 열림 중 idle 개입 차단. |
| 2 | **모바일에서 타이머가 맨 아래.** GridStack 1열 재배치가 카드를 뒤섞어, 가장 중요한 타이머/시작 카드가 빈 카드 6개(2340px) 아래에 묻힘. | 중대 | 1열일 때 우선순위 정렬(타이머→트리→Todo→계획→오늘→어제→달력). 데스크톱 레이아웃은 캡처·복원. |
| 3 | 복귀 모달에 `"10minUnit"` 표기(존재하지 않는 i18n 키). | 중 | `fmtGap`을 정상 기간 단위(분/시간)로 수정, `durHourUnit` 키 추가(en/ko/vi). |
| 4 | 영문 부재중 문구 `"1 minutes"` 복수형 오류. | 소 | 숫자 무관한 `"min"` 표기로 변경. |
| 5 | 내일 계획에 **현재 작업 중인 작업/그 부모**가 "미완료"로 표시됨. | 중 | 활성 작업과 그 조상 체인을 플랜에서 제외. |
| 6 | 내일 계획 항목을 처리(오늘 시작/+Todo)해도 목록에 계속 남음. | 중 | 처리 시 플랜에서 자동 제거(`planDismissed`, 클라우드 동기화). +버튼 추가. |
| 7 | 복귀 모달 문구가 active 케이스에서 "일시정지됨"으로 부정확. | 소 | "자리를 비웠어요" 중립 표현으로 변경. |
| 8 | **하위작업 추가 발견성 없음(P1).** 계층이 `Switch`+드래그로만 생성돼 처음 쓰는 사람이 알기 어려움. | 높음 | 트리 노드마다 hover `+` 버튼 추가 → 해당 노드의 하위작업으로 모달을 엶. |
| 9 | **`Switch`의 강제 중첩(P1).** 무관한 작업도 무조건 현재 작업의 하위로 들어가 트리가 의도치 않게 깊어짐. | 높음 | 시작 모달에 `[독립 작업] / [○○의 하위작업]` 토글 추가. 기본값 = 독립 작업. 트리 `+`로 열면 하위 기본. |

| 10 | 캘린더가 활동 유무만 점으로 표시(집중도 파악 불가). | 중 | 날짜별 집중시간에 따라 빨강(<2h)/노랑(2–4h)/초록(4h+) 색상 + 범례. |
| 11 | **휴식 미종료 퇴근 처리(요청).** 휴식 상태로 퇴근 후 집에서 접속하면 그 시간이 전부 휴식으로 기록됨. | 중 | 퇴근시각 초과 감지 시 모달: [퇴근시각에 종료] 또는 [휴식 시간 직접 입력] → 정리 후 자동 퇴근. |
| 12 | 하루 작업 흐름을 시각적으로 볼 수단 없음(SIDEKICK 참고). | 신규 | "오늘 타임라인" 간트 위젯 추가: 시간축 + 작업별 색상 막대(실제 시각 반영, 휴식 주황, 진행중 막대 점멸). |
| 13 | Daily Report PDF가 화면 양식과 다르게(구조·시간·상태 없이 평평하게) 출력됨. | 중 | PDF에 트리 들여쓰기·상태 아이콘·항목별 시간·취소선·집중/휴식 요약·A4 인쇄 CSS 반영. |
| 14 | **뽀모도로 부재.** 집중 타이머 앱인데 뽀모도로 자동순환이 없음. | 높음 | 뽀모도로 자동순환 타이머 신규 구현(work→휴식→work, N사이클마다 긴 휴식, 사이클 표시, 설정 가능). |
| 15 | **Pro 전환 전략 부재.** 유료 차별점이 얇고(위젯/광고), 전환 동기가 없음. | 높음 | **전략 전환**: 뽀모도로는 무료(습관·리텐션 훅). Pro 핵심 훅 = **집중 인사이트**(추세·연속·시간대 패턴). 무료는 실제 총계·작업별은 보되, 고급 분석은 블러+잠금+업그레이드 CTA(가치 우선 업셀). pricing/upgrade 재포지셔닝. |
| 16 | 전환 유도 접점 추가. | 높음 | ① **주간 넛지 배너**(무료, 3일+ 활동 시 주 1회 "이번 주 X 집중 🔥 인사이트 보기"). ② **PDF 리포트 → Pro 전환**(무료는 텍스트 복사 유지, 버튼 🔒 + 업그레이드 안내). ③ **인앱 월간 회고 배너**(새 달 첫 접속 시 "지난달 X·N일" → 인사이트). *월간 이메일 발송은 백엔드(Supabase 함수+이메일 서비스) 필요 → 후속 과제.* |

---

## 남은 개선 제안 (미반영 · 우선순위순)

### P1 (후속) — 타임라인 드래그 편집
- 타임라인 막대를 드래그해 기록 시각을 수정하는 기능. 시간 기록을 되쓰는 작업이라 정확성·되돌리기 설계가 필요해 별도 진행.

### P2 — 모바일 빈 카드 높이
- **현상**: 모바일에서 각 카드가 내용과 무관하게 ~350px 고정 → 빈 위젯도 화면을 꽉 채워 스크롤 과다.
- **제안**: 모바일에서 카드 높이를 내용에 맞게 축소(min-height 완화), 또는 비어있는 저우선 위젯 접기.

### P2 — "Tomorrow's plan (unfinished)" 위젯명 이중 의미
- **현상**: 이름은 "내일 계획"인데 실제로는 **오늘 멈춘 작업 + 어제 미완료**를 보여줌. 하루 중에는 "지금 멈춰둔 작업 목록"으로 읽혀 혼란.
- **제안**: 명칭을 "이어서 할 작업" 등으로 조정하거나, 상단에 설명 문구 추가.

### P3 — 라이브 리사이즈 전환 매끄러움
- **현상**: 데스크톱↔모바일 폭 경계를 실시간으로 오갈 때 그리드가 잠깐 흐트러질 수 있음(리로드 시 정상). 신규 로드는 양쪽 모두 정상.
- **제안**: GridStack 컬럼 전환 이벤트에 맞춘 레이아웃 복원 정교화(필요 시 전용 모바일 레이아웃 검토).

---

## 검증 메모
- 인라인 스크립트 전체 `node --check` 통과.
- 브라우저 실사용으로 시간초과·부재중·복귀보정·모바일 순서·플랜 제외를 직접 재현·확인.
- 백엔드(Supabase) 정상, 로그인 정상 확인.
